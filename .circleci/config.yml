version: 2
jobs:
  build:
    docker:
      - image: alpine
    steps:
      - checkout
      - run:
          name: helm-github-pages
          environment:
            - GITHUB_PAGES_REPO: xmple/helm-charts
          command: wget -O - https://raw.githubusercontent.com/xmple/helm-github-pages/master/publish.sh | sh